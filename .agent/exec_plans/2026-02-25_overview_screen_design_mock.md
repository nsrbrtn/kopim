# ExecPlan: Дизайн экрана обзора по макету

## Context and Orientation
- Цель: реализовать статичный экран `OverviewScreen` по предоставленному образцу для перехода с карточки «Общий баланс» на главном экране.
- Область кода: `lib/features/overview/presentation/overview_screen.dart`, `docs/components/`.
- Контекст/ограничения: пока без рабочей бизнес-логики и вычислений; использовать цвета и типографику текущей темы.
- Риски: визуальные расхождения с макетом на узких экранах и возможные overflow.

## Interfaces and Dependencies
- Внешние сервисы: нет.
- Локальные зависимости: `ThemeData`, `ColorScheme`, `AppLocalizations`.
- Затрагиваемые API/модули: маршрут `OverviewScreen.routeName` (без изменения контракта).

## Plan of Work
- Перестроить `OverviewScreen` как прокручиваемую колонку карточек по макету.
- Сохранить текущий маршрут и переход с Home.
- Обновить документацию компонента экрана обзора.

## Concrete Steps
1) Подготовить каркас `OverviewScreen` с отступами и заголовком.
2) Добавить статичные карточки и декоративные элементы по образцу.
3) Обновить документацию и прогнать проверки формата/анализатора.

## Validation and Acceptance
- Команды проверки:
```bash
dart format --set-exit-if-changed lib/features/overview/presentation/overview_screen.dart docs/components/HomeOverviewSummaryCard.md
flutter analyze lib/features/overview/presentation/overview_screen.dart
```
- Acceptance criteria:
  - Экран открывается по текущему маршруту `/overview` из карточки «Общий баланс».
  - Визуальная структура соответствует образцу (карточки, иерархия, отступы, прогресс-бары).
  - Цвета и шрифты берутся из темы.

## Idempotence and Recovery
- Безопасно перезапускать: `dart format`, `flutter analyze`.
- Откат: вернуть изменения в `overview_screen.dart` и документации.
- Rollback миграций: не требуется.

## Progress
- [x] Шаг 1: Создать ExecPlan.
- [x] Шаг 2: Реализовать статичный дизайн экрана.
- [x] Шаг 3: Обновить документацию и выполнить проверки.

## Surprises & Discoveries
- Экран `OverviewScreen` до начала задачи содержал только текстовую заглушку.

## Decision Log
- Для ускорения реализации выбран статичный макет без источников данных и без обработчиков действий.
- Цвета взяты из `ColorScheme` (тема приложения), а не захардкожены как в макете Figma.

## Outcomes & Retrospective
- Что сделано:
  - Полностью реализован UI экрана обзора по структуре из образца.
  - Сохранён текущий маршрут и переход с карточки «Общий баланс» на Home.
  - Обновлена документация компонента карточки обзора.
- Что бы улучшить в следующий раз:
  - Добавить привязку к реальным данным и интерактивность действий (настройки/метрики).
