# UpcomingSummaryBadges

## Назначение
Компонент для отображения сводной информации о предстоящих платежах и напоминаниях на главном экране. Показывает один или несколько бейджей с количеством событий или сообщение о пустом состоянии.

> **Примечание**: Это приватный виджет (`_UpcomingSummaryBadges`), используемый внутри карточки предстоящих платежей на главном экране.

## Параметры

| Параметр | Тип | Описание | По умолчанию |
|----------|-----|----------|--------------|
| `paymentCount` | `int` | Количество предстоящих платежей | *обязательный* |
| `reminderCount` | `int` | Количество активных напоминаний | *обязательный* |
| `strings` | `AppLocalizations` | Объект локализации для получения переведённых строк | *обязательный* |
| `theme` | `ThemeData` | Тема приложения для стилизации | *обязательный* |

## Особенности поведения

- **Условное отображение**: показывает только те бейджи, у которых счётчик больше 0
- **Пустое состояние**: если оба счётчика равны 0, отображает текст "Нет предстоящих событий"
- **Цветовое кодирование**:
  - Платежи — основной цвет темы (`primary`)
  - Напоминания — вторичный цвет темы (`secondary`)
- **Адаптивная компоновка**: использует `Wrap` для автоматического переноса бейджей на новую строку при необходимости
- **Локализация**: все тексты и семантические метки получаются из `AppLocalizations`

## Логика отображения

```
если platformCount > 0 → показать бейдж "Платежи"
если reminderCount > 0 → показать бейдж "Напоминания"
если оба = 0 → показать "Нет предстоящих событий"
```

## Пример использования

```dart
// С обоими типами событий
_UpcomingSummaryBadges(
  paymentCount: 3,
  reminderCount: 2,
  strings: AppLocalizations.of(context)!,
  theme: Theme.of(context),
)
// Отобразится: [🔄 3 Платежи] [⏰ 2 Напоминания]

// Только платежи
_UpcomingSummaryBadges(
  paymentCount: 5,
  reminderCount: 0,
  strings: AppLocalizations.of(context)!,
  theme: Theme.of(context),
)
// Отобразится: [🔄 5 Платежи]

// Пустое состояние
_UpcomingSummaryBadges(
  paymentCount: 0,
  reminderCount: 0,
  strings: AppLocalizations.of(context)!,
  theme: Theme.of(context),
)
// Отобразится: "Нет предстоящих событий"
```

## Связанные файлы

- Исходный код: [`home_upcoming_items_card.dart`](file:///home/artem/StudioProjects/kopim/lib/features/home/presentation/widgets/home_upcoming_items_card.dart#L145-L199)
- Дочерний виджет: [`_UpcomingBadge`](UpcomingBadge.md)
- Родительский компонент: карточка предстоящих платежей на главном экране
