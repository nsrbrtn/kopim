# BudgetsEmptyState

## Назначение
Виджет пустого состояния для экрана бюджетов. Отображается когда у пользователя ещё не создано ни одного бюджета.

> **Примечание**: Это приватный виджет (`_BudgetsEmptyState`), используемый внутри экрана бюджетов.

## Параметры

| Параметр | Тип | Описание | По умолчанию |
|----------|-----|----------|--------------|
| `strings` | `AppLocalizations` | Объект локализации для получения переведённых строк | *обязательный* |

## Особенности поведения

- **Центрированная компоновка**: содержимое размещается по центру экрана
- **Иконка**: крупная иконка круговой диаграммы (`pie_chart_outline`) основного цвета
- **Заголовок и сообщение**: два текстовых блока с информацией о пустом состоянии
- **Адаптивные отступы**: использует систему spacing из `KopimLayout`
- **Адаптивный размер иконки**: размер иконки берётся из `iconSizes.xl`

## Визуальная структура

```
┌─────────────────┐
│                 │
│       🥧        │ ← иконка (XL)
│                 │
│   Заголовок     │ ← titleMedium
│                 │
│   Описание      │ ← bodyMedium
│                 │
└─────────────────┘
```

## Пример использования

```dart
// Используется внутри экрана бюджетов
if (budgets.isEmpty) {
  return _BudgetsEmptyState(
    strings: AppLocalizations.of(context)!,
  );
}
```

## Связанные файлы

- Исходный код: [`budgets_screen.dart`](file:///home/artem/StudioProjects/kopim/lib/features/budgets/presentation/budgets_screen.dart#L160-L199)
- Родительский экран: `BudgetsScreen`
